/* Masotto Core V69: incasso manuale booking + flussi previsionali + Airbnb tax handling */
(function(){
  const PAGES = [
    ["index.html","Dashboard","layout-dashboard"],["anagrafica.html","Anagrafica","id-card"],["asset.html","Asset","boxes"],["audit.html","Audit","clipboard-check"],["manutenzione.html","Manutenzione","wrench"],["prenotazioni.html","Prenotazioni","calendar"],["finanze.html","Finanze","pie-chart"],["sicurezza.html","Sicurezza","shield"]
  ];
  const LS_MAP = { masotto_prop_data:'property_master', masotto_assets_mobile_db:'assets_mobile', masotto_structural_assets_db:'structural_assets', masotto_finance_db:'finances', masotto_booking_db:'bookings', masotto_maint_db:'tickets', masotto_insurance_db:'insurance', masotto_utilities_db:'utilities', masotto_contacts_db:'contacts', masotto_maintenance_presets_db:'maintenance_presets', masotto_supply_presets_db:'supply_presets' };
  const STATUS = { paid:'Eseguito', pagata:'Eseguito', pagato:'Eseguito', done:'Eseguito', executed:'Eseguito', pending:'Aperto', open:'Aperto', previsto:'Previsto', planned:'Previsto', forecast:'Previsto', accantonato:'Accantonato', accrued:'Accantonato' };
  function toISODate(d){ if(!d) return ''; const x=new Date(d); if(isNaN(x)) return ''; return x.toISOString().slice(0,10); }
  function addDays(dateStr, days){ const d=new Date(dateStr+'T00:00:00'); d.setDate(d.getDate()+Number(days||0)); return d.toISOString().slice(0,10); }
  function n(v){ return Number(v||0) || 0; }
  function lower(v){ return String(v||'').toLowerCase(); }
  function defaultCleaningFee(nights){ return Number(nights||0)>14 ? 80 : 45; }
  function normalizeStatusLabel(s){ return STATUS[lower(s)] || s || 'Previsto'; }
  function statusCode(s){ const l=normalizeStatusLabel(s); if(l==='Eseguito') return 'paid'; if(l==='Accantonato') return 'accrued'; if(l==='Aperto') return 'pending'; return 'planned'; }
  function bookingCheckIn(b){ return b.check_in || b.checkin || b.checkIn || ''; }
  function bookingCheckOut(b){ return b.check_out || b.checkout || b.checkOut || addDays(bookingCheckIn(b), b.nights || 1); }
  function bookingCleaning(b){ return n(b.cleaning_fee_eur ?? b.cleaning_eur ?? b.clean ?? b.cleaning) || defaultCleaningFee(b.nights); }
  function bookingRoom(b){ const cleaning=bookingCleaning(b); if(b.room_amount_eur!=null) return n(b.room_amount_eur); if(b.camera_eur!=null) return n(b.camera_eur); if(b.gross_eur!=null) return Math.max(0,n(b.gross_eur)-cleaning); if(b.price!=null) return Math.max(0,n(b.price)-cleaning); return 0; }
  function bookingTaxable(b){ if(b.taxable_gross_eur!=null) return n(b.taxable_gross_eur); if(b.cedolare_base_eur!=null) return n(b.cedolare_base_eur); if(b.receipt_total_display_eur!=null) return n(b.receipt_total_display_eur); if(b.gross_eur!=null) return n(b.gross_eur); if(b.price!=null) return n(b.price); return bookingRoom(b)+bookingCleaning(b); }
  function bookingCityTax(b){ return n(b.city_tax_eur ?? b.tax ?? b.city_tax); }
  function bookingIsAirbnb(b){ return lower(b.source||b.platform||b.channel).includes('airbnb'); }
  function bookingFinancialUnknown(b){ return b.financial_summary_inclusion==='excluded_until_total_verified' || lower(b.data_quality)==='total_to_verify' || lower(b.status).includes('amount_to_verify'); }
  function bookingCedolareWithheldByPlatform(b){
    if(b.cedolare_withheld_by_platform===false) return false;
    if(b.cedolare_withheld_by_platform===true) return true;
    if(lower(b.cedolare_handling).includes('withheld') || lower(b.cedolare_handling).includes('tratten')) return true;
    return bookingIsAirbnb(b);
  }
  function bookingCityTaxWithheldByPlatform(b){
    if(b.city_tax_withheld_by_platform===false) return false;
    if(b.city_tax_withheld_by_platform===true) return true;
    if(lower(b.city_tax_handling).includes('withheld') || lower(b.city_tax_handling).includes('tratten') || lower(b.city_tax_handling).includes('platform')) return true;
    if(!bookingIsAirbnb(b)) return false;
    const gross=b.guest_total_eur ?? b.gross_collected_eur;
    const accommodation=b.accommodation_gross_eur ?? b.taxable_gross_eur;
    const payout=b.expected_host_payout_eur;
    const fee=b.host_service_fee_eur ?? b.ota_commission_eur;
    const city=bookingCityTax(b);
    if(gross!=null && accommodation!=null && city>0 && Math.abs((n(gross)-n(accommodation))-city)<0.02){
      if(payout==null || fee==null || Math.abs((n(payout)+n(fee))-n(accommodation))<0.02) return true;
    }
    return false;
  }
  function bookingCollected(b, existingAutoById){
    const ci=bookingCheckIn(b); const bid=b.id||stableId('BK',(b.guest||'Ospite')+ci);
    const old=existingAutoById && existingAutoById[`AUTO-INC-${bid}`];
    if(old && statusCode(old.status)==='paid') return true;
    const collection=lower(b.collection_status||b.collection_state||'');
    if(['received','paid','collected','incassata','incassato'].includes(collection)) return true;
    if(b.payment_confirmed===true || b.cash_received_confirmed===true || b.payout_status==='paid') return true;
    if(b.payment_confirmed===false || collection==='pending') return false;
    const st=lower(b.status);
    return st==='completed' || st.startsWith('completed_');
  }
  function bookingCashIncome(b){
    if(bookingFinancialUnknown(b)) return 0;
    if(bookingIsAirbnb(b)){
      if(b.expected_host_payout_eur!=null) return n(b.expected_host_payout_eur);
      if(b.cash_received_eur!=null) return n(b.cash_received_eur);
      if(b.wise_movement_eur!=null) return n(b.wise_movement_eur);
      return bookingTaxable(b);
    }
    if(b.guest_total_eur!=null) return n(b.guest_total_eur);
    if(b.gross_collected_eur!=null) return n(b.gross_collected_eur);
    if(b.cash_received_eur!=null) return n(b.cash_received_eur);
    if(b.wise_movement_eur!=null && n(b.wise_movement_eur)>0) return n(b.wise_movement_eur)+bookingCityTax(b);
    return bookingTaxable(b)+bookingCityTax(b);
  }
  function getPaymentAccount(tx){
    const date = tx.date || ''; const cat=lower(tx.category||tx.cat); const desc=lower(tx.description||tx.desc); const cutoff='2025-12-31';
    if((cat.includes('tasse')||cat.includes('tax')) && desc.includes('cedolare')) return 'Fondo fiscale';
    if(date && date <= cutoff){ if(desc.includes('imu')||desc.includes('tari')) return 'Da verificare'; return 'Wise'; }
    if(cat.includes('utenze')){ if(desc.includes('luce')||desc.includes('electricity')||desc.includes('gas')) return 'Fineco'; if(desc.includes('internet')||desc.includes('fastweb')) return 'Wise'; }
    if(cat.includes('pulizie')||cat.includes('manutenzione')||cat.includes('straordinaria')||cat.includes('assicurazione')||cat.includes('consumabili')||cat.includes('gestione ordinaria')||cat.includes('partite di giro')) return 'Wise';
    if(cat.includes('condominio')||cat.includes('ordinaria')){ if(desc.includes('prima rata 2026/27')||desc.includes('1 rata 2026/27')) return 'Wise'; return 'Fineco'; }
    return tx.payment_account || tx.account || 'Da verificare';
  }
  function normalizeStatus(tx){ return normalizeStatusLabel(tx.status); }
  function stableId(prefix, value){ let h=0; const s=String(value); for(let i=0;i<s.length;i++){ h=((h<<5)-h)+s.charCodeAt(i); h|=0; } return prefix+'-'+Math.abs(h); }
  function generateBookingRows(bookings, today=new Date(), existingAutoById={}){
    const rows=[];
    (bookings||[]).forEach(b=>{ const ci=bookingCheckIn(b); if(!ci) return; const co=bookingCheckOut(b); const guest=b.guest||'Ospite'; const bid=b.id||stableId('BK',guest+ci); const cleaning=bookingCleaning(b); const taxable=bookingTaxable(b); const city=bookingCityTax(b); const room=bookingRoom(b); const fiscalYear=String((co||ci).slice(0,4)); const collected=bookingCollected(b,existingAutoById); const unknown=bookingFinancialUnknown(b); const airbnb=bookingIsAirbnb(b); const cedWithheld=bookingCedolareWithheldByPlatform(b); const cityWithheld=bookingCityTaxWithheldByPlatform(b); const cashIncome=bookingCashIncome(b); const cedolare=Math.round(taxable*0.21*100)/100;
      rows.push({ id:`AUTO-INC-${bid}`, date:ci, description:`${collected?'Incasso':'Importo in arrivo'} soggiorno: ${guest}`, category:'Prenotazione', type:'income', amount_eur:unknown?0:cashIncome, income:unknown?0:cashIncome, expense:0, status:collected?'paid':'pending', payment_account:airbnb?'Airbnb':'Wise', booking_id:bid, is_auto_generated:true, fiscal_year:ci.slice(0,4), room_amount_eur:room, cleaning_fee_eur:cleaning, taxable_eur:unknown?null:taxable, fiscal_income_eur:unknown?null:taxable, city_tax_eur:city, riccardo_direct_eur:n(b.riccardo_direct_eur), forecast_only:!collected, manual_collection_required:!collected, amount_to_verify:unknown, receipt_rule:'Movimento cassa attivato solo alla conferma manuale di incasso; prenotazioni future restano previsionali.' });
      rows.push({ id:`AUTO-CLEAN-${bid}`, date:co, description:`Pulizie: ${guest}`, category:'Pulizie', type:'expense', amount_eur:cleaning, income:0, expense:cleaning, status:collected?'pending':'planned', payment_account:'Wise', booking_id:bid, is_auto_generated:true, fiscal_year:fiscalYear, forecast_only:!collected, activated_by_collection:collected, notes:collected?'Obbligo generato dalla conferma di incasso; segnare Pagato solo al pagamento effettivo.':'Previsione: non e una uscita reale finche la prenotazione non viene segnata Incassata.' });
      if(cedWithheld) rows.push({ id:`AUTO-CED-${bid}`, date:co, description:`Cedolare Secca trattenuta da Airbnb: ${guest} / ${fiscalYear}`, category:'Tasse', type:'memo', amount_eur:unknown?0:cedolare, income:0, expense:0, status:collected?'paid':'planned', payment_account:'Airbnb', booking_id:bid, is_auto_generated:true, fiscal_year:fiscalYear, platform_handled:true, exclude_from_cash:true, exclude_from_local_tax_reserve:true, forecast_only:!collected, notes:'Airbnb trattiene la cedolare: importo informativo, non sottrarre nuovamente dalla liquidita e non creare accantonamento locale.' });
      else rows.push({ id:`AUTO-CED-${bid}`, date:co, description:`Accantonamento Cedolare Secca: ${guest} / ${fiscalYear}`, category:'Tasse', type:'accrual', amount_eur:unknown?0:cedolare, income:0, expense:unknown?0:cedolare, status:collected?'accrued':'planned', payment_account:'Fondo fiscale', booking_id:bid, is_auto_generated:true, fiscal_year:fiscalYear, forecast_only:!collected, activated_by_collection:collected, amount_to_verify:unknown });
      if(city>0){ if(cityWithheld) rows.push({ id:`AUTO-CITY-${bid}`, date:ci, description:`Tassa soggiorno trattenuta/gestita da Airbnb: ${guest}`, category:'Partite di giro', type:'memo', amount_eur:city, income:0, expense:0, status:collected?'paid':'planned', payment_account:'Airbnb', booking_id:bid, is_auto_generated:true, fiscal_year:ci.slice(0,4), city_tax_eur:city, platform_handled:true, exclude_from_cash:true, exclude_from_pnl:true, forecast_only:!collected }); else rows.push({ id:`AUTO-CITY-${bid}`, date:ci, description:`Versamento tassa soggiorno: ${guest}`, category:'Partite di giro', type:'expense', amount_eur:city, income:0, expense:city, status:collected?'pending':'planned', payment_account:'Wise', booking_id:bid, is_auto_generated:true, fiscal_year:ci.slice(0,4), city_tax_eur:city, exclude_from_pnl:true, forecast_only:!collected, activated_by_collection:collected, notes:collected?'Obbligo generato dopo incasso. Segnare Pagato solo quando il riversamento e effettivo.':'Previsione: nessuna uscita finche la prenotazione non viene segnata Incassata.' }); }
      const reimbursements=Array.isArray(b.riccardo_reimbursements)?b.riccardo_reimbursements:[]; reimbursements.forEach((r,idx)=>{ const reimbAmount=n(r.amount_eur??r.amount??0); if(reimbAmount>0){ const reimbDate=r.date||co||ci; rows.push({id:`AUTO-RICCARDO-${bid}-${idx+1}`,date:reimbDate,description:r.description||`Giroconto a Riccardo: ${guest} tranche ${idx+1}`,category:'Giroconto Riccardo',type:'expense',amount_eur:reimbAmount,income:0,expense:reimbAmount,status:collected?'pending':'planned',payment_account:'Wise',booking_id:bid,is_auto_generated:true,fiscal_year:reimbDate.slice(0,4),forecast_only:!collected}); }});
    }); return rows;
  }
  function monthRange(start,end){const out=[];let d=new Date(start+'T00:00:00');const e=new Date(end+'T00:00:00');while(d<=e){out.push(d.toISOString().slice(0,7));d.setMonth(d.getMonth()+1);}return out;}
  function generateMonthlyDefaults(finances,today=new Date()){const rows=[];const months=monthRange('2025-01-01',today.toISOString().slice(0,10));const exists=(desc,ym)=>(finances||[]).some(f=>lower(f.description||f.desc).includes(desc)&&String(f.date||'').startsWith(ym));months.forEach(ym=>{const date=ym+'-28';if(!exists('internet',ym)&&!exists('fastweb',ym))rows.push({id:'AUTO-INTERNET-'+ym,date,description:'internet Fastweb mensile',category:'Utenze',type:'expense',amount_eur:27.95,income:0,expense:27.95,status:'paid',payment_account:'Wise',is_auto_generated:true,fiscal_year:ym.slice(0,4)});if(ym>='2025-08'&&!exists('consumabili gestione ordinaria',ym))rows.push({id:'AUTO-CONS-'+ym,date,description:'consumabili gestione ordinaria',category:'Gestione ordinaria',type:'expense',amount_eur:30,income:0,expense:30,status:'paid',payment_account:'Wise',is_auto_generated:true,fiscal_year:ym.slice(0,4)});});return rows;}
  function normalizeFinanceRows(finances,bookings,today=new Date()){const existingAutoById={};(finances||[]).forEach(f=>{const id=String(f.id||'');if(id.startsWith('AUTO-'))existingAutoById[id]=f;});const manual=(finances||[]).filter(f=>!String(f.id||'').startsWith('AUTO-')).filter(f=>!lower(f.description||f.desc).trim().startsWith('bookings ')).map(f=>{const out={...f};out.description=out.description||out.desc||'';out.category=out.category||out.cat||'Altro';out.amount_eur=n(out.amount_eur??out.amount??out.expense??out.out);const lt=lower(out.type);out.type=lt==='revenue'?'income':(out.type||(out.category==='Entrata Extra'?'income':'expense'));out.status=statusCode(normalizeStatus(out));out.payment_account=getPaymentAccount(out);out.fiscal_year=String(out.related_year||out.fiscal_year||(out.date||'').slice(0,4));return out;});const generated=[...generateBookingRows(bookings,today,existingAutoById),...generateMonthlyDefaults(manual,today)].map(g=>{const old=existingAutoById[String(g.id)];if(old){const oldStatus=statusCode(old.status);if(g.type!=='income'&&old.user_confirmed_status===true)g.status=oldStatus;if(g.type==='income'&&oldStatus==='paid')g.status='paid';if(g.type==='expense'&&oldStatus==='paid')g.status='paid';g.payment_account=old.payment_account||g.payment_account;g.notes=old.notes||g.notes;if(old.status_changed_at)g.status_changed_at=old.status_changed_at;if(old.user_confirmed_status===true)g.user_confirmed_status=true;}return g;});const generatedIds=new Set(generated.map(g=>String(g.id||'')));const preservedAuto=Object.values(existingAutoById).filter(f=>String(f.id||'').startsWith('AUTO-OPENING-')||String(f.id||'').startsWith('AUTO-YEAREND-')).filter(f=>!generatedIds.has(String(f.id||''))).map(f=>{const out={...f};out.type=out.type||'memo';out.amount_eur=n(out.amount_eur??out.amount??0);out.status=statusCode(out.status||'paid');out.payment_account=out.payment_account||'Wise';return out;});return [...manual,...preservedAuto,...generated];}
  function confirmBookingCollection(financeRowId){const id=String(financeRowId||'');const finances=JSON.parse(localStorage.getItem('masotto_finance_db')||'[]')||[];const tx=finances.find(x=>String(x.id)===id);const bid=tx&&tx.booking_id!=null?String(tx.booking_id):id.replace(/^AUTO-INC-/,'');const bookings=JSON.parse(localStorage.getItem('masotto_booking_db')||'[]')||[];const idx=bookings.findIndex(b=>String(b.id)===bid);if(idx<0)return {ok:false,error:'Prenotazione non trovata'};const now=new Date().toISOString();bookings[idx]={...bookings[idx],collection_status:'received',payment_confirmed:true,collected_at:now};if(bookingIsAirbnb(bookings[idx]))bookings[idx].payout_status='paid';localStorage.setItem('masotto_booking_db',JSON.stringify(bookings));const normalized=normalizeFinanceRows(finances,bookings,new Date());localStorage.setItem('masotto_finance_db',JSON.stringify(normalized));return {ok:true,booking:bookings[idx],finances:normalized};}
  function getYearEndBalance(year,opts={}){const master=window.MASOTTO_DB||{};const bookings=(opts.bookings||JSON.parse(localStorage.getItem('masotto_booking_db')||'null')||master.bookings||[]).map(b=>({...b}));const finances=(opts.finances||JSON.parse(localStorage.getItem('masotto_finance_db')||'null')||master.finances||[]).map(f=>({...f}));const rows=normalizeFinanceRows(finances,bookings,opts.today?new Date(opts.today):new Date());const y=String(year);const yearRows=rows.filter(t=>String(t.fiscal_year||(t.date||'').slice(0,4))===y);const bookingRows=generateBookingRows(bookings).filter(t=>String(t.fiscal_year||(t.date||'').slice(0,4))===y);const receipts=bookings.filter(b=>String(bookingCheckIn(b)).slice(0,4)===y&&bookingCollected(b)&&!bookingFinancialUnknown(b)).map(b=>{const taxable=bookingTaxable(b),cleaning=bookingCleaning(b),city=bookingCityTax(b),room=bookingRoom(b);return {id:b.id,guest:b.guest||'Ospite',check_in:bookingCheckIn(b),check_out:bookingCheckOut(b),nights:Number(b.nights||0),pax:Number(b.pax||1),soggiorno_eur:room,pulizie_eur:cleaning,imponibile_cedolare_eur:taxable,tassa_soggiorno_eur:city,totale_incassato_eur:bookingCashIncome(b),cedolare_21_eur:Math.round(taxable*21)/100,cedolare_withheld_by_platform:bookingCedolareWithheldByPlatform(b),city_tax_withheld_by_platform:bookingCityTaxWithheldByPlatform(b)};});const sum=(arr,fn)=>Math.round(arr.reduce((a,x)=>a+Number(fn(x)||0),0)*100)/100;const isPaid=t=>statusCode(t.status)==='paid';const isOut=t=>{const type=String(t.type||'').toLowerCase();if(type==='memo'||type==='accrual'||!isPaid(t))return false;return type==='expense'||type==='uscita'||Number(t.expense||0)>0;};const isIn=t=>{const type=String(t.type||'').toLowerCase();if(!isPaid(t))return false;return type==='income'||type==='entrata'||Number(t.income||0)>0;};const localCedolare=yearRows.filter(t=>String(t.type).toLowerCase()==='accrual'&&statusCode(t.status)==='accrued'&&!t.exclude_from_local_tax_reserve);const platformCedolare=yearRows.filter(t=>t.platform_handled&&lower(t.description).includes('cedolare'));const categories={};yearRows.filter(isOut).filter(t=>!t.exclude_from_pnl).forEach(t=>{const k=t.category||'Altro';categories[k]=Math.round(((categories[k]||0)+Number(t.amount_eur||t.expense||0))*100)/100;});return {year:y,generated_at:new Date().toISOString(),receipts,totals:{receipts_count:receipts.length,imponibile_cedolare:sum(receipts,r=>r.imponibile_cedolare_eur),tassa_soggiorno_riscossa_host:sum(receipts.filter(r=>!r.city_tax_withheld_by_platform),r=>r.tassa_soggiorno_eur),tassa_soggiorno_gestita_piattaforma:sum(receipts.filter(r=>r.city_tax_withheld_by_platform),r=>r.tassa_soggiorno_eur),pulizie_previste:sum(receipts,r=>r.pulizie_eur),cedolare_accantonata:sum(localCedolare,t=>t.amount_eur||t.expense),cedolare_trattenuta_piattaforma:sum(platformCedolare,t=>t.amount_eur||0),entrate_reali:sum(yearRows.filter(isIn),t=>t.amount_eur||t.income),uscite_reali:sum(yearRows.filter(isOut),t=>t.amount_eur||t.expense)},categories,rows:yearRows,generated_booking_rows:bookingRows};}
  function receiptPageHtml(r,prop){const eur=v=>Number(v||0).toLocaleString('it-IT',{minimumFractionDigits:2,maximumFractionDigits:2});const fmt=d=>d?String(d).split('-').reverse().join('/'):'N/D';const propName=prop.name||'Masotto Terrace View';const propAddress=prop.address||'Via Privata Umberto Masotto 4, 20133 Milano';const propOwner=prop.owner||'Armati Riccardo';const propCIR=prop.cir||'015146-LNI-09408';const soggiorno=Math.max(0,Number(r.imponibile_cedolare_eur||0)-Number(r.pulizie_eur||0));const receiptDate=r.issue_date||r.check_in;const cityLabel=r.city_tax_withheld_by_platform?'Tassa di soggiorno trattenuta/gestita dalla piattaforma':'Tassa di soggiorno riscossa dal locatore';const cedNote=r.cedolare_withheld_by_platform?'<p>Cedolare secca gestita/trattenuta da Airbnb: non viene creato un secondo accantonamento locale.</p>':'';return `<section class="receipt-page"><div class="receipt-topline">${new Date().toLocaleString('it-IT')}<span>Ricevuta_${r.id}</span></div><header class="receipt-header"><div><div class="receipt-kicker">RICEVUTA NON FISCALE - LOCAZIONE BREVE TURISTICA</div><h1>RICEVUTA DI PAGAMENTO</h1><p><strong>N. Rif:</strong> ${r.id}</p><p>Data emissione: ${fmt(receiptDate)}</p></div><div class="property-box"><h2>${propName}</h2><p>${propAddress}</p><p>Proprietario: ${propOwner}</p><p>CIR: ${propCIR}</p></div></header><div class="receipt-rule"></div><div class="receipt-info-grid"><div class="receipt-info-box"><h3>DATI OSPITE</h3><p><strong>Nome:</strong> ${r.guest||''}</p><p><strong>Ospiti totali:</strong> ${r.pax||1}</p></div><div class="receipt-info-box"><h3>DETTAGLI SOGGIORNO</h3><p><strong>Check-in:</strong> ${fmt(r.check_in)}</p><p><strong>Check-out:</strong> ${fmt(r.check_out)}</p><p><strong>Totale Notti:</strong> ${r.nights||0}</p></div></div><table class="receipt-table"><thead><tr><th>DESCRIZIONE</th><th>IMPORTO</th></tr></thead><tbody><tr><td>Soggiorno (${r.nights||0} notti)</td><td>€ ${eur(soggiorno)}</td></tr><tr><td>Spese di pulizia</td><td>€ ${eur(r.pulizie_eur)}</td></tr><tr class="total-row"><td>Totale lordo per cedolare secca</td><td>€ ${eur(r.imponibile_cedolare_eur)}</td></tr><tr><td>${cityLabel}</td><td>€ ${eur(r.tassa_soggiorno_eur)}</td></tr></tbody></table><div class="law-notes"><p>Locazione breve turistica ai sensi dell'art. 4 DL 50/2017.</p>${cedNote}</div></section>`;}
  function receiptsPrintCss(){return `<style>body{font-family:Arial;color:#111827}.receipt-page{width:210mm;min-height:297mm;margin:auto;padding:18mm 20mm}.receipt-header{display:flex;justify-content:space-between}.receipt-kicker{font-weight:800}.receipt-header h1{color:#00585f}.receipt-rule{border-top:3px solid #00585f;margin:30px 0}.receipt-info-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.receipt-info-box{border:1px solid #d8e2ee;padding:20px}.receipt-table{width:100%;border-collapse:collapse;margin-top:40px}.receipt-table td,.receipt-table th{padding:12px;border-bottom:1px solid #cbd5e1}.receipt-table td:last-child,.receipt-table th:last-child{text-align:right}.total-row{font-weight:800}@media print{@page{size:A4;margin:0}}</style>`;}
  function printReceipt(bookingId){const master=window.MASOTTO_DB||{};const bookings=JSON.parse(localStorage.getItem('masotto_booking_db')||'null')||master.bookings||[];const b=bookings.find(x=>String(x.id)===String(bookingId));if(!b){alert('Prenotazione non trovata.');return null;}if(!bookingCollected(b)){alert('La prenotazione non risulta ancora incassata.');return null;}if(bookingFinancialUnknown(b)){alert('Importo prenotazione da verificare.');return null;}const taxable=bookingTaxable(b),cleaning=bookingCleaning(b),city=bookingCityTax(b),room=bookingRoom(b);const r={id:b.id,guest:b.guest||'Ospite',check_in:bookingCheckIn(b),check_out:bookingCheckOut(b),nights:Number(b.nights||0),pax:Number(b.pax||1),soggiorno_eur:room,pulizie_eur:cleaning,imponibile_cedolare_eur:taxable,tassa_soggiorno_eur:city,cedolare_withheld_by_platform:bookingCedolareWithheldByPlatform(b),city_tax_withheld_by_platform:bookingCityTaxWithheldByPlatform(b)};const prop=(master.property_master&&master.property_master[0])||{};const html=`<!doctype html><html><head>${receiptsPrintCss()}</head><body>${receiptPageHtml(r,prop)}<script>window.onload=()=>window.print()<\/script></body></html>`;const w=window.open('','_blank');if(w){w.document.write(html);w.document.close();}return r;}
  function printAllReceipts(year){const report=getYearEndBalance(year);const prop=(window.MASOTTO_DB&&window.MASOTTO_DB.property_master&&window.MASOTTO_DB.property_master[0])||{};const html=`<!doctype html><html><head>${receiptsPrintCss()}</head><body>${report.receipts.map(r=>receiptPageHtml(r,prop)).join('')}<script>window.onload=()=>window.print()<\/script></body></html>`;const w=window.open('','_blank');if(w){w.document.write(html);w.document.close();}return report;}
  function getYearClosures(){try{return JSON.parse(localStorage.getItem('masotto_year_closures')||'{}')||{};}catch(e){return {};}}function getYearClosure(y){return getYearClosures()[String(y)]||null;}function saveYearClosure(y,c){const all=getYearClosures();all[String(y)]={...(all[String(y)]||{}),...c};localStorage.setItem('masotto_year_closures',JSON.stringify(all));return all[String(y)];}function markF24Paid(y,file){const c=getYearClosure(y);if(!c)return null;return saveYearClosure(y,{...c,tax_reserve_status:'pagato',f24_paid:true,f24_file_name:file||'',f24_paid_at:new Date().toISOString()});}
  function closeFiscalYear(year){const report=getYearEndBalance(year);return report;}
  function getCedolareAccantonata(rows,year){return (rows||[]).filter(t=>statusCode(t.status)==='accrued'&&String(t.type||'').toLowerCase()==='accrual'&&!t.exclude_from_local_tax_reserve&&String(t.fiscal_year||(t.date||'').slice(0,4))===String(year)).reduce((a,t)=>a+n(t.amount_eur??t.expense),0);}function getExpenseByCategory(rows,year,q){const qq=lower(q);return (rows||[]).filter(t=>String(t.fiscal_year||(t.date||'').slice(0,4))===String(year)&&lower((t.category||'')+' '+(t.description||'')).includes(qq)).reduce((a,t)=>a+n(t.amount_eur??t.expense),0);}
  async function ensureMasterDB(force=false){try{const master=window.MASOTTO_DB||{};for(const [lsKey,section] of Object.entries(LS_MAP)){const existing=localStorage.getItem(lsKey);const empty=!existing||existing==='null'||existing==='[]'||existing==='{}'||existing==='undefined';if(force||empty)localStorage.setItem(lsKey,JSON.stringify(master[section]??[]));}return true;}catch(e){return false;}}
  function mountSidebar(){if(document.getElementById('msSidebar'))return;}
  window.MS_ACCOUNTING={defaultCleaningFee,bookingCheckIn,bookingCheckOut,bookingCleaning,bookingTaxable,bookingRoom,bookingCityTax,bookingCashIncome,bookingIsAirbnb,bookingCollected,bookingCedolareWithheldByPlatform,bookingCityTaxWithheldByPlatform,getPaymentAccount,normalizeStatus,statusCode,normalizeFinanceRows,generateBookingRows,confirmBookingCollection,getYearEndBalance,printReceipt,printAllReceipts,closeFiscalYear,getYearClosure,getYearClosures,saveYearClosure,markF24Paid,getCedolareAccantonata,getExpenseByCategory};
  window.msReady=async function(force=false){await ensureMasterDB(force);mountSidebar();return true;};
  if((location.pathname.split('/').pop()||'').toLowerCase()==='finanze.html'){const s=document.createElement('script');s.src='finance_v69_patch.js?v=20260823-v69';s.defer=true;document.head.appendChild(s);}
  document.addEventListener('DOMContentLoaded',()=>{mountSidebar();});
})();
